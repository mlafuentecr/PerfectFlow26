import React from 'react'; import { ScrollView, StyleSheet, View } from 'react-native'; import { LinearGradient } from 'expo-linear-gradient'; import { NativeStackScreenProps } from '@react-navigation/native-stack'; import { RootStackParamList } from '../App'; import Header from '../components/Header'; import AppCard from '../components/AppCard'; import { logout } from '../services/auth';
type Props = NativeStackScreenProps<RootStackParamList, 'home'>;
export default function HomeScreen({ navigation }: Props) { return <LinearGradient colors={['#020D35','#041A5E','#041746']} style={{flex:1}}><ScrollView contentContainerStyle={s.c}><Header title='PerfectFlow' /><AppCard title='Start Breathing' description='Begin a guided breathing exercise.' onPress={()=>navigation.navigate('breathing')} /><AppCard title='Daily Insight' description='A short calming thought for today.' onPress={()=>navigation.navigate('insights')} /><AppCard title='Mood Reset' description='Choose how you feel and start a session.' onPress={()=>navigation.navigate('sessions')} /><AppCard title='Weekly Progress' description='Track your breathing and calm moments.' onPress={()=>navigation.navigate('progress')} /><AppCard title='Logout' description='Sign out from your account.' onPress={async ()=>{await logout(); navigation.replace('index');}} /></ScrollView></LinearGradient>; }
const s=StyleSheet.create({c:{padding:20,paddingTop:56,paddingBottom:40}});
